function isBigger(num) {
    if (num > 5) {
        return true;
    }
    else {

        return false
    }
}
const res = isBigger(43)

// const sum = a, b=> a + 3
const sum = (a, b) => a + b
const resalt = sum(4, 3)
// console.log(resalt); 

const mask = {
    name: "elon",
    age: 43,
    job: "tesla"
}
// console.log(mask.job); 
const numbers = [, 43, 43, 434, 34, 12, 213, 23, 34]
// console.log(numbers); 

for (let i = 0; i < 10; i++) {
    // const n= numbers[i 
    const n = numbers[i]
}

// if (numbers.length>5)&&(numbers[0]====5){


// }